#Create an array where the array[index] contains the factorial of index
def createFactorialArray(z):
    return None

#With a grid n*n, count all possible paths with at least k turns
def countPaths(n,k):
    factorials = createFactorialArray(n-2)